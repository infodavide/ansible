Pour utiliser la fonction sudo sur les serveurs avec votre compte bureautique:

ansible-vault create var_pass.yml -> créer un fichier cripté 
saisir la variable ansible_sudo_pass: "votre_mot_de_passe_bureautique"

ansible-vault edit var_pass.yml -> modifier le fichier cripté 


